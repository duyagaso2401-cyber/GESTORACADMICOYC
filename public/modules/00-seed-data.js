// 00-seed-data.js — stub vacío
// Los datos de prueba (SEED_DATA) fueron eliminados para evitar interferencia
// con la base de datos real de Neon conectada al backend en Render.
// _seedDBIfEmpty es un no-op que devuelve los datos tal como llegan del servidor.
const SEED_DATA = {};
function _seedDBIfEmpty(data){ return data; }
