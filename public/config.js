// API_URL vacío = URLs relativas → el servidor Express de este mismo
// despliegue maneja /api/inetis/events y /api/inetis/notify localmente
// y proxea el resto al backend Neon (gestoracadmicoyc.onrender.com).
const CONFIG = {
    API_URL: ""
};
