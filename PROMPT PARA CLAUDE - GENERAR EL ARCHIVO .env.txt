¡Hola, Adán! Por acá todo excelente, amaneciendo con la mejor energía para dejar ese sistema a punto.
Aquí tienes el prompt maestro reestructurado y potenciado. Está diseñado para exigirle a Claude que haga una auditoría profunda línea por línea de todo el código de "Gestor Académico YC", verifique cada llamada a process.env y te entregue la plantilla exacta del archivo .env comentada:
Hola Claude, necesito que realices una AUDITORÍA INTEGRAL DE CÓDIGO FUENTE en todo el proyecto "Gestor Académico YC" para mapear y estructurar la CONFIGURACIÓN DEFINITIVA DE VARIABLES DE ENTORNO que deben configurarse en el panel de Render y en el entorno local.

Revisa línea por línea cada módulo backend, controladores, servicios (EcosystemAgent, auth, base de datos, sincronización) y utilidades de envío de notificaciones (Nodemailer/Zoho Mail) donde se invoque `process.env`.

======================================================================
1. GENERACIÓN DEL ARCHIVO .env COMPLETO Y ESTRUCTURADO
======================================================================
Genera el contenido exacto para el archivo `.env` (o panel de Environment Variables en Render) bajo la siguiente estructura y requisitos:

- COMENTARIOS EXPLICATIVOS: Cada variable debe tener un comentario justo encima que explique:
  1. ¿Para qué sirve y en qué archivo/función se utiliza dentro del sistema?
  2. ¿Qué valor exacto debe llevar si es un parámetro estático/estándar?
  3. Si es un valor privado o dinámico que requiere estar vacío, indica la RUTA EXACTA O PLATAFORMA donde se obtiene o genera (ej. Consola de Zoho Mail, Google AI Studio, Neon PostgreSQL, etc.).

- BLOQUES REQUERIDOS EN EL ARCHIVO:
  * SERVIDOR Y ENTORNO: `PORT`, `NODE_ENV`, claves secretas de sesión/JWT (`JWT_SECRET`, etc.).
  * BASE DE DATOS NEON POSTGRESQL: `DATABASE_URL` (para el Pooler de Render/producción) y cualquier parámetro de conexión individual si existiera.
  * AGENTE AUTÓNOMO E IA (GEMINI): `GEMINI_API_KEY` y banderas de configuración o modelo asignado (`gemini-2.5-flash`).
  * NOTIFICACIONES Y CORREOS REALES (ZOHO MAIL / SMTP):
    - Servidor SMTP (`SMTP_HOST` = smtp.zoho.com / smtp.zoho.com.co).
    - Puerto SMTP (`SMTP_PORT` = 465 o 587).
    - Usuario/Correo emisor (`SMTP_USER` / `MAIL_FROM`).
    - Contraseña de aplicación de Zoho (`SMTP_PASS`).
    - Banderas de entorno para habilitar/deshabilitar notificaciones reales a docentes, coordinadores, estudiantes y rectoría.
  * SINCRONIZACIÓN Y NODOS OFFLINE-FIRST: Secretos o tokens de seguridad para validación entre el servidor central y los nodos locales rurales.

======================================================================
2. REVISIÓN Y CONFIRMACIÓN DE FUNCIONALIDAD REAL
======================================================================
- AUDITORÍA DE NOTIFICACIONES: Verifica si dentro del código del sistema existe alguna bandera de pruebas, modo mock, simulación en consola o condición (`if (process.env.NODE_ENV === 'development')`) que esté bloqueando o interceptando el envío real de correos a las bandejas de entrada de los usuarios. Si existe, indícame qué variable debo incluir o modificar en Render para forzar el envío REAL en producción.
- VALIDACIÓN CRUZADA DE SEGURIDAD: Confirma que no exista ninguna variable crítica omitida que cause que el servidor Express falle al arrancar (`crash`) al desplegarse en Render.

Entrega el bloque de código limpio listo para copiar y pegar dentro del archivo `.env` o en las variables de entorno de Render.